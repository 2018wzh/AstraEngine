schema: astra.scenario.v1
id: nativevn.route.library
stage: stage3-astra-vn
package: target/nativevn.astrapkg
target: nativevn-game
profile: advanced-vn
platform: headless
generated_route_id: route.nativevn.library
seed: 6101
locale: en
actions:
  - launch: {}
  - player_input: { kind: complete_wait, value: timeline.hero.enter.complete }
  - player_input: { kind: complete_wait, value: movie.signal.end }
  - player_input: { kind: complete_wait, value: voice.cc0.sample.end }
  - player_input: { kind: advance, ticks: 1 }
  - player_input: { kind: complete_wait, value: voice_end }
  - player_input: { kind: advance, ticks: 1 }
  - player_input: { kind: choose, value: choice.library }
  - player_input: { kind: advance, ticks: 1 }
  - player_input: { kind: open_system, value: backlog }
  - player_input: { kind: save, slot: slot.library }
  - player_input: { kind: load, slot: slot.library }
assertions:
  - coverage:
      routes: [state.ending_library]
      backlog_keys: [technical.intro, technical.library]
      read_state: [line.technical.intro, line.technical.library]
      voice_replay: [voice.cc0.sample]
  - check: vn.advanced_presentation
  - check: timeline.join_cancel
  - check: presentation.fallback
  - check: voice.sync
  - check: renderer.effect_budget
  - replay_hash_match: true
  - no_blocking_diagnostics: true
