schema: astra.scenario.v1
id: nativevn.route.rooftop
stage: stage3-astra-vn
package: target/nativevn.astrapkg
target: nativevn-game
profile: advanced-vn
platform: headless
generated_route_id: route.nativevn.rooftop
seed: 6102
locale: en
actions:
  - launch: {}
  - player_input: { kind: complete_wait, value: timeline.hero.enter.complete }
  - player_input: { kind: complete_wait, value: movie.signal.end }
  - player_input: { kind: complete_wait, value: voice.cc0.sample.end }
  - player_input: { kind: advance, ticks: 1 }
  - player_input: { kind: complete_wait, value: voice_end }
  - player_input: { kind: advance, ticks: 1 }
  - player_input: { kind: choose, value: choice.rooftop }
  - player_input: { kind: advance, ticks: 1 }
  - player_input: { kind: open_system, value: localization_preview }
  - player_input: { kind: set_config, key: locale_preview, value: en }
  - player_input: { kind: save, slot: slot.rooftop }
  - player_input: { kind: load, slot: slot.rooftop }
assertions:
  - coverage:
      routes: [state.ending_rooftop]
      backlog_keys: [technical.intro, technical.rooftop]
      read_state: [line.technical.intro, line.technical.rooftop]
      voice_replay: [voice.cc0.sample]
  - check: vn.advanced_presentation
  - check: timeline.join_cancel
  - check: presentation.fallback
  - check: voice.sync
  - check: renderer.effect_budget
  - system_state:
      config: { locale_preview: en }
  - no_blocking_diagnostics: true
