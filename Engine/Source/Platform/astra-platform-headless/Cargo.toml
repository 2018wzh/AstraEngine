[package]
name = "astra-platform-headless"
edition.workspace = true
license.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false

[features]
default = []
ffmpeg-vcpkg = ["astra-media/ffmpeg-vcpkg"]

[dependencies]
astra-core = { path = "../../Runtime/astra-core" }
astra-headless-protocol = { path = "../../Runtime/astra-headless-protocol" }
astra-media = { path = "../../Runtime/astra-media" }
astra-media-core = { path = "../../Runtime/astra-media-core" }
# Headless is publish=false test infrastructure.  It must consume the same
# test-driver command enum as the native Windows E3 driver so Cargo feature
# unification cannot leave its host loop non-exhaustive.
astra-platform = { path = "../astra-platform", features = ["platform-test-driver"] }
astra-platform-common = { path = "../astra-platform-common" }
astra-ui-core = { path = "../../Runtime/astra-ui-core" }
image.workspace = true
hound.workspace = true
postcard.workspace = true
reqwest.workspace = true
sha2.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio = { workspace = true, features = ["io-util", "net"] }
tracing.workspace = true
url.workspace = true
tempfile.workspace = true

[dev-dependencies]
rcgen.workspace = true
tokio-rustls.workspace = true
