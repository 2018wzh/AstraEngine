# AstraEngine 工作宪章

本宪章根据项目所有者 2026-09-05 的产品问卷修订。目标是让 VN 创作者使用现代、演出强化、高可定制的引擎充分表达创作意图，同时交付实用的旧 VN 兼容播放器。简化工程负担，不简化掉创作能力。

## 产品与近期重点

近期有两个独立交付闭环：AstraVN 支撑《终之空》现代化高清重制完整可玩；AstraEMU 运行选定的 Minori 引擎游戏。Minori 是当前 EMU 优先级，FVP 和 Artemis 不再是这两项交付的前置条件。已有其他 family 能力保留，但不为其扩张本轮范围。

Windows、Android 为 T0；Web、iOS 为 T1；Linux、macOS 为 T2。T0 表示当前产品优先保障的平台，不表示每次文档或纯逻辑修改都要跑两个平台的全部测试。T1 按相关变更和平台迭代验证，T2 不阻断 T0 交付；没有验证的平台如实标记。

NativeVN、EMU 独立开发、验证、发布，共享引擎能力但不要求全系列 lockstep release。Editor、运行时 AI、RPG/TRPG、公开第三方插件生态是长期方向，不阻断近期产品。项目没有固定短期截止日期；每轮仍围绕一个可验证的产品结果推进。

产品细节见 [产品愿景](Docs/product/vision.md)、[路线图](Docs/product/roadmap.md)和[已确认决策](Docs/product/decisions-2026-09-05.md)。这些页面与本宪章取代旧文档中冲突的优先级、统一发布和强制治理规则；旧 contract 的数据布局、算法事实与兼容约束不会因文字更新自动失效。修改相应实现时同步修订其直接相关文档，不要求先重写全部历史资料。

## Agent 的自主范围

在上述目标内默认自主调查、实施、调试和验证，不为普通工程选择反复请求批准。可以删除死代码、合并或拆分模块、调整 crate/依赖/配置、替换已无价值的测试、修改错误的 gate，并在没有真实消费者的阶段进行破坏性 API/格式升级。采取能闭环的方案，不只提交设计框架或占位接口。

自主性不等于扩大产品范围。不要擅自改变作品剧情或核心美术意图，也不要把长期方向升级为当前前置条件。遇到信息缺口，先查仓库和现有资料，再选局部、可逆的默认方案并披露；只有未给出的外部凭据、授权、不可逆用户数据操作或真正无法推断的创作决定才需要提问。授权范围仍受当前工具权限限制。

正常改动只需在提交或交付中说明改变了什么、为何更简单、验证了什么及未验证项。不要求每个修改配套 ADR、schema、迁移协议、release gate、coverage matrix 和人工签字。跨 ABI/存档格式等会影响调用者的变化，保留一份足够说明影响的迁移记录即可。

## 设计原则

- 优先复用现有可靠代码和成熟库。普通模块、函数和数据结构足以解决的问题，不新增 provider、registry、profile 或通用框架。没有固定 crate 数量、文件行数、抽象层数或第二消费者门槛；按实际职责、构建隔离、平台/FFI 边界判断。
- 保持剧情状态和存档的单一权威。Actor/Component + StateMachine、`.astra`、Luau、wgpu、Yakui、Slint 是当前实现起点，不是不可调整的教条；改进它们应服务真实用例。不要为简化实现把演出系统降成固定皮肤、固定对话框或仅有少量硬编码效果的播放器。
- Core 不承担具体作品脚本、Editor UI、某个旧引擎私有语义或平台资源生命周期。原生资源归实际使用它们的 host/provider 管理；只在确有隔离收益的边界使用公共 ABI。内部调用不必一律经过动态插件、序列化 DTO 或通用注册中心。
- 多层场景、图像与文本样式、自定义 UI、转场、时间线、音视频协调和资源替换按作品需求组合。Graph/Timeline 与文本源保持可追溯的关系，但完整 Editor 不成为 CLI/文本创作与发布的前置。作品特定逻辑放在作品配置、脚本或适配器，不塞入通用运行时。
- 默认优化一个活跃游戏会话的简单执行路径。GPU、音频、解码和 I/O 可以使用必要的持久 worker；不为尚未确认的多会话规模强制引入 server/mailbox/broker。不得用每次 step 新建并 join 线程的方式包装同步调用。
- 热路径避免无收益的重复编码、全量扫描、整帧回读和内容哈希。边界完整性校验、廉价的生命周期/顺序/范围校验继续保留；不能把所有 clone、hash 或检查一概禁用，也不能以 Shipping 为名改变正确性语义。优化用相关场景的测量证明，不统一强制 120 Hz、指定 GPU 或特定分析工具。

## 可信扩展与故障处理

当前原生插件、Luau、游戏资源、Family 插件和 AI 输出按可信协作组件设计，不以抵御恶意插件为默认任务。无需强制沙箱、进程隔离、签名 allowlist、每次调用的 capability 握手或多层身份 envelope。可信脚本可按创作需求使用 host 服务；平台权限和实际资源配置仍然有效，不为取消旧限制而自动增加文件/网络能力。

可信不代表不会损坏或出错。保留内存/FFI 所有权、长度与算术边界、文件读写、格式识别、请求超时、外部响应大小、存档完整性等必要检查。对调用方传来的错误只在负责处置的边界记录，不沿栈重复包装同一错误。Warning 不应因放进 diagnostics 列表就自动成为 Blocking。

原生 ABI 的布局和对象生命周期不兼容仍必须拒绝。仅为诊断而保存的编译器/feature 指纹不强制精确匹配；但使用普通 Rust dylib、布局条件变化或实际 ABI 限制时，应按对应链接方式的真实兼容条件处理，不能把所有指纹差异一律降级。没有需要时不实现热重载；卸载前必须能证明不再使用该库的函数和析构器，否则让库存活到会话或进程结束。

允许有语义依据的默认配置和 fallback，例如缺字字体链、用户选择的解码兼容路径、翻译失败保留原文。必要能力缺失应明确报错；不能用空输出、假资源、假成功或悄悄丢剧情来掩盖失败。超时返回和关闭路径不得等待正在超时的任务所持有的锁。

## 存档、跨端与云同步

同版本冷启动读档是基本能力。数据设计支持跨语言、跨设备、跨平台转移及后续云同步；它不要求全世界历史 trace、跨硬件逐像素 hash 一致或分布式会话框架。使用稳定作品/资源标识，不把本地绝对路径、指针或 native handle 写入可移植存档。

目前没有真实用户和下游消费者，可以改 API、package/save/schema 并明确拒绝旧版本，不默认建设长期兼容层。可破坏性升级不等于可以自动删除用户或研究所需的旧文件。云端读写、覆盖、删除需要在实际启用时处理账户、授权和冲突；当前先保证可移植数据及明确版本，不宣称同步服务已经完成。

读档失败时关闭当前游戏会话并返回安全入口，给出错误；不要求失败后继续原会话，也不要求为此复制整个世界做昂贵回滚。不得继续运行半恢复状态，不覆盖已有有效存档。写入采用足够保证有效旧存档不会被截断的临时写入与原子替换；一个会话失败不应无故结束 Manager 或其他无关会话。

回放、证据采集和调试记录按实际需求启用，不作为每次运行的默认强制账本。无需为未启用的 AI 产品建设审计协议；需要回放时记录足以重现结果的已提交输入/输出，不重新调用外部模型。

## 测试与完成标准

测试按其证明的行为分层。纯逻辑使用普通 `#[test]`、`#[tokio::test]` 和有价值的 doctest；组件测试允许 FakeClock、内存 VFS、mock provider 或录制响应；只有图形、音频、输入、生命周期和真实整合需要真实 host。Headless 是工具，不是所有测试的入口，也不是发行平台证明。

主动削减重复、只验证类型存在/名称/固定常量、空 helper、旧架构形式和大量重叠全流程测试。保留或合并能捕获真实回归的测试，优先覆盖剧情推进、分支、冷启动读档、失败关闭、数据解析与真实平台接入。不设测试数量或覆盖率配额，不为减少数量删除仍然有效的唯一保护；用删除原因和相关行为的验证结果说明即可。

局部修改运行影响范围内的最小充分检查；共享 API、存档、平台或依赖变化再扩大范围。文档修改不要求全 workspace Clippy/test、GPU/音频或六平台验收；变更 Python 检查器时运行它的定向测试。大型验收保留给产品里程碑和相关高风险变更，不必每次提交重跑。

《终之空》现代化重制以完整可玩、创作意图、高清呈现、文本清晰、音视频与交互正确为目标，不自动继承 Classic 的逐像素/固定 SSIM 门禁。保留真实剧情/资源正确性和代表性视觉核对。Agent 可以有依据地合并场景、调整测量方式或更新与新设计一致的基线，说明原因与前后差异；不能仅因失败而降低阈值、删失败样本或改写旧结果。不强制人工 tolerance approval 或每项人工签字。

成功声明只对应实际执行的行为。普通测试、mock、目录存在、静态报告都不能冒充真实 Headless、完整游戏、Android 真机或跨平台成功。检查脚本退出码为 0 只说明该脚本检查通过，不说明其没有执行的产品行为通过。

## 日常工程与资料

使用已有 Rust 工具链、格式化和日志设施；新依赖按用途评估，不要求建立新的治理系统。库负责结构化事件，入口负责日志生命周期。公开报告不含凭据、未授权商业素材或私有路径；本地调试可以保留定位问题所需的路径和内容，但不得自动上传或混入发行包。合法授权的作品资源可以进入作品包，不能把报告脱敏规则错误套用到作品本身。

并行 Agent 修改同一仓库时使用独立 worktree/分支，避免覆盖他人修改或并发写同一构建产物；不为只读调查强制新建复杂会话。只清理自己的临时产物，不擅自删除他人的文件、分支或数据。

文档中文主体，代码标识保留原名。设计、已实现和待验证明确区分，允许 TODO、draft 和未决定项诚实存在。只维护直接受影响的说明和索引，不为形式完整而复制多份状态表。禁止把“增加更多规则”当作所有工程问题的默认解法。
