{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "LegacySystemMenuRequestV1",
  "type": "object",
  "required": [
    "action",
    "sequence"
  ],
  "properties": {
    "action": {
      "$ref": "#/definitions/LegacySystemMenuActionV1"
    },
    "item_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "menu_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "pointer_x": {
      "type": [
        "integer",
        "null"
      ],
      "format": "int32"
    },
    "pointer_y": {
      "type": [
        "integer",
        "null"
      ],
      "format": "int32"
    },
    "sequence": {
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    }
  },
  "definitions": {
    "LegacySystemMenuActionV1": {
      "description": "Typed semantic request emitted by a host when the physical secondary pointer button opens the family-owned system menu.",
      "type": "string",
      "enum": [
        "open",
        "select",
        "dismiss"
      ]
    }
  }
}
