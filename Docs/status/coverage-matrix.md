# Implementation Coverage Matrix

| 模块 | Design | Contract | Public API | Data Format | Test Scenario | Release Gate | Manual |
| --- | --- | --- | --- | --- | --- | --- | --- |
| EngineCore | [module](../modules/engine-core.md) | [runtime](../contracts/runtime.md) | `Engine/Source/Runtime/astra-runtime` implemented, `Engine/Source/Runtime/astra-engine` Rust dylib facade implemented | runtime save uses shared `astra-package` container | `native_smoke.yaml`, `astra-cli` logging test, `cargo test -p astra-runtime --test state_machine_tick`, `cargo test -p astra-runtime --test await_token`, `cargo test -p astra-engine dylib_facade` | runtime determinism, flat StateMachine validation, Await/Fence materialization, structured logs, Rust dylib facade | [operator](../manual/operator-guide.md) |
| Target Model | [architecture](../product/architecture.md) | [data](../contracts/data-formats.md) | `Engine/Source/Platform/astra-target` implemented, [target/platform](../implementation/target-platform.md) | `project.yaml targets`, `astra.target_manifest.v1` package section | `cargo test -p astra-target`, `cargo test -p astra-cli --test target_platform` | target manifest | [operator](../manual/operator-guide.md) |
| Plugin ABI | [architecture](../product/architecture.md) | [plugin](../contracts/plugin-abi.md) | `Engine/Source/Runtime/astra-plugin-abi` and `Engine/Source/Runtime/astra-plugin` implemented, [provider API](../implementation/provider-plugin-api.md) | plugin YAML, extension registry, dependency graph | plugin load/unload, FFI action, extension registry and logging scenario | fingerprint/capability, `plugin.extension_registry`, `plugin.dependency_graph`, plugin structured logs | [plugin guide](../manual/plugin-developer-guide.md) |
| Asset Pipeline | [module](../modules/asset-pipeline.md) | [data](../contracts/data-formats.md) | `Engine/Source/Runtime/astra-asset`, `Engine/Source/Developer/astra-cook`, `Engine/Source/Runtime/astra-package` implemented | asset sidecar YAML, cook manifest, binary package | `cargo test -p astra-asset sidecar_schema`, `cargo test -p astra-cook import_cook`, `cargo test -p astra-package package_roundtrip` | package integrity, `package.cooked_project` release input | [creator](../manual/creator-manual.md) |
| Media Runtime | [module](../modules/media-runtime.md) | [media](../contracts/media.md) | `Engine/Source/Runtime/astra-media-core` 提供轻量 Renderer2D/FilterGraph contract 和 deterministic CPU executor；`Engine/Source/Runtime/astra-media` re-export core contract 并实现 text/decode/audio providers | FilterGraph YAML, FilterExecutionReport, AudioGraph state, decode capability report, `Engine/Fixtures/PublicDomainMedia/manifest.json` | `cargo test -p astra-media headless_capture`, `text_layout`, `audio_graph`, `filter_graph`, `decode_provider` | decode/provider gate, public fixture hash validation, deterministic headless frame/filter hash, WMF audio/video decode evidence | [operator](../manual/operator-guide.md) |
| AstraVN | [module](../modules/astra-vn.md), [script spec](../modules/astra-vn-script.md), [presentation](../modules/astra-vn-presentation-model.md), [commands](../modules/astra-vn-standard-commands.md), [system UI](../modules/astra-vn-system-ui-profile.md) | [script-vn](../contracts/script-vn.md), [player automation](../implementation/astra-vn-live-player-automation.md) | `Engine/Source/Runtime/astra-vn` in progress；`astra-cli` 已能 cook/package NativeVN、AdvancedVN、synthetic TsuiNoSora project 和 demo-slice 生成的 TsuiNoSora NativeVN project，生成 Windows/Web standalone bundle，并输出 player route report；`astra-player-core` 和 `astra-player` 已实现 live input transcript/report validator，route report 仍不能冒充 `player.full_playable`；`astra-test` 已接入 VN player route、`complete_wait` 和 advanced assertion；`astra-release` 已接入 `vn.*`、`tsuinosora.*` 与 `player.full_playable` gate；`astra-cli` 支持 project-level `package_sections` target/profile 过滤和 NativeVN asset sidecar cook；`Tools/TsuiNoSora` 已提供 `internal-demo-bundle` 私有 acceptance 入口 | `.astra`、`CompiledStory`、`VnRuntimeState`、`VnRuntimeStateSave`、`VnPolicyState`、`VnPolicyStateSave`、`VnPolicyBundleManifest`、`VnPolicyBundleSourceCache`、`VnExtensionManifest`、`VnStandardCommandManifest`、`VnPresentationProviderManifest`、`VnCommercialBaselineManifest`、`VnAdvancedPresentationManifest`、`StageModel`、`SystemStoryManifest`、`VnSystemUiProfileManifest`、Editor metadata、`astra.standalone_bundle_manifest.v1`、`astra.player_launch_report.v1`、`astra.player_route_report.v1`、`astra.player_automation_script.v1`、`astra.player_input_transcript.v1`、`astra.player_automation_report.v1`、`asset.registry.assets`、`scenario_json_refs`、`AstraPlayer.route_model.json`、`AstraPlayer.mount_policy.json`、`package_sections` descriptor、`vn.compiled_story`、`vn.profile_manifest`、`vn.policy_bundle_manifest`、`vn.policy_bundle_source_cache`、`vn.extension_manifest`、`vn.standard_command_manifest`、`vn.presentation_provider_manifest`、`vn.commercial_baseline_manifest`、`vn.advanced_presentation_manifest`、`vn.system_story_manifest`、`vn.system_ui_profile_manifest`、`Examples/NativeVN/project.yaml`、`Examples/AdvancedVN/project.yaml`、`scenarios/full_playthrough.yaml`、`scenarios/advanced_presentation.yaml`、`mount_probes`、`mount_assets`、`tsuinosora.projectorrays_reader_report.v1`、`tsuinosora.projectorrays_full_dump_report.v1`、`tsuinosora.extract_report.v1`、`tsuinosora.director_resource_map.v1`、`tsuinosora.director_cast_map.v1`、`tsuinosora.cast_source_map_report.v1`、`tsuinosora.script_source_map.v1`、`tsuinosora.script_source_map_report.v1`、`tsuinosora.route_graph_report.v1`、`tsuinosora.reference_evidence`、`tsuinosora.asset_analysis`、`tsuinosora.conversion_manifest`、`tsuinosora.mount_policy`、`tsuinosora.modern_profile_report`、`tsuinosora.manual_signoff`、`tsuinosora.nativevn_package_input_report.v1`、`tsuinosora.local_gate_report.v1`、`tsuinosora.demo_slice_report.v1`、`tsuinosora.internal_demo_bundle_report.v1` | `cargo test -p astra-media --test filter_graph`、`cargo test -p astra-vn`、`cargo test -p astra-vn --test policy_bundle`、`cargo test -p astra-vn --test compiler_diagnostics`、`cargo test -p astra-vn --test presentation_model`、`cargo test -p astra-vn --test presentation_execution`、`cargo test -p astra-vn --test advanced_presentation`、`cargo test -p astra-vn --test await_gates`、`cargo test -p astra-vn --test standard_command_manifest`、`cargo test -p astra-vn --test commercial_baseline_manifest`、`cargo test -p astra-vn --test vn_dylib_facade`、`cargo test -p astra-vn --test vn_plugin_extensions`、`cargo test -p astra-vn --test commercial_baseline`、`cargo test -p astra-vn --test luau_sandbox`、`cargo test -p astra-vn --test luau_mutation`、`cargo test -p astra-vn --test system_controls`、`cargo test -p astra-vn --test vn_save_container`、`cargo test -p astra-release --test release_report release_gate_`、`cargo test -p astra-release --test release_report tsuinosora`、`cargo test -p astra-release release_gate_accepts_player_full_playable_only_with_matching_live_report`、`cargo test -p astra-player-core`、`cargo test -p astra-player --test windows_input_automation`、`cargo test -p astra-player --test web_input_automation`、`cargo test -p astra-test --test vn_scenario`、`cargo test -p astra-cli --test target_platform nativevn_sample_cooks_packages_validates_and_runs_full_playthrough`、`cargo test -p astra-cli --test target_platform advanced_vn_sample_runs_opt_in_presentation_gate`、`cargo test -p astra-cli --test target_platform nativevn_sample_builds_windows_and_web_bundles_and_runs_player_routes`、`cargo test -p astra-cli --test target_platform tsuinosora_synthetic_gate_runs_internal_and_patch_player_routes`、`cargo test -p astra-cli --test target_platform tsuinosora_demo_slice_generates_playable_nativevn_and_player_routes`、`cargo test -p astra-cli --test target_platform tsuinosora_internal_demo_builds_asset_package_and_bundles`、`python Tools/TsuiNoSora/tests/test_asset_analysis.py`、`python Tools/TsuiNoSora/tests/test_asset_analysis.py -k internal_demo_bundle`、`python Tools/TsuiNoSora/tests/test_asset_analysis.py -k projectorrays_full_dump`、`python Tools/TsuiNoSora/tests/test_asset_analysis.py -k full_resource_conversion` | `vn.compiled_story`、`vn.profile_manifest`、`vn.policy_bundle`、`vn.extension_bindings`、`vn.standard_commands`、`vn.presentation_provider`、`vn.commercial_baseline`、`vn.system_ui_profile`、`vn.advanced_presentation`、`tsuinosora.reference_evidence`、`tsuinosora.asset_analysis`、`tsuinosora.conversion_manifest`、`tsuinosora.mount_policy`、`tsuinosora.modern_profile_report`、`tsuinosora.manual_signoff` required check set 和 `player.full_playable` 已有 release gate slice；official policy source cache、NativeVN full playthrough、AdvancedVN opt-in scenario、真实 VN cdylib provider fixture、Luau command/query/trace capability、Runtime save container VN sections、FilterGraph CPU execution、Windows/Web bundle route report、bundle mount policy route/hash check、Windows `mount_probes`/route-bound `mount_assets` + `--mount-root` local probe/asset、synthetic TsuiNoSora internal/patch 路线 report、`demo-slice --config` 私有 root 入口、`internal-demo-bundle` 私有 acceptance 入口、生成 project 的 internal Windows/Web route 和 patch Windows direct-read、ProjectorRays 脱敏 reader report、ProjectorRays full dump `chunk_fourcc_counts`/`conversion_plan` 和全量 binary chunk conversion blocker、NativeVN asset sidecar/cooked asset/asset registry、Director `imap`/`mmap` resource map preflight、Director `KEY*`/`CAS*` cast map preflight、可读或短 binary-header wrapped mapped `Lscr` 内部 source-map sidecar、受限 RIFF/RIFX chunk reader、cast source map report、脱敏 script source map sidecar/report、route graph report、NativeVN package input file/hash manifest、extract preflight 未解析 Director container 阻断、Asset analysis script reference/duplicate hash/color/conflict quarantine 和 local-gate orchestrator 已有 evidence；ProjectorRays raw chunk 到 NativeVN asset/command 的真实 converter、真实 `.local` bundle 产物、真实平台 host run、TsuiNoSora commercial full gate、formal signoff、modern/Patch/VFS 后续计划仍未完成 | [creator](../manual/creator-manual.md) |

补充：AstraVN/TsuiNoSora coverage 中的 `tsuinosora.cast_source_map_report.v1` 当前覆盖两条公开 synthetic 路径：手写 `tsuinosora.cast_map.v1` 的 source/container entry/hash 映射，以及从 Director `KEY*`/`CAS*` cast map 通过 child resource id、FourCC、container entry id 和 extracted payload hash 派生的 source map；手写或外部 reader sidecar 声明 `source_hash` 时，必须匹配实际 extracted source asset，否则 blocking。`tsuinosora.route_graph.v1` 当前覆盖脱敏 route/terminal/choice sidecar，带正文类字段或不安全 symbol 时必须 blocking。`tsuinosora.script_source_map.v1` 当前覆盖外部 reader 产出的脱敏 route/source/line/hash sidecar，以及 `extract-readable` 对可读或短 binary-header wrapped mapped `Lscr` 自动生成的 `director_lingo_source_map.json`；带正文、bytecode、本地路径、无效 hash、不安全 symbol、不安全 reader id/hash/output contract、声明 source hash 与现有 report-relative source 文件不一致、route line 超出声明 source line_count、未声明 route source 或 route/source hash 不一致时必须 blocking。同一 route 同时来自抽出的 `.ls` 文本和 reader sidecar 时，`tsuinosora.script_source_map_report.v1` 优先保留 reader source-map evidence 和脱敏 reader id/hash evidence，避免重复 coverage。`tsuinosora.director_lingo_map.v1` 覆盖 `Lctx`/`Lnam`/`Lscr` 的 hash-only preflight；`Lnam` 只输出 entry count 和 table hash，不输出 symbol/name 字符串；`Lscr` 不是可直接抽取文本且没有合规 source-map sidecar 用匹配的 `director_lingo_map.json` source/hash 和 Lscr `script_resource_id`/`script_payload_sha256` 证明 route coverage 时，`tsuinosora.script_source_map_report.v1` 必须因 unsupported Lingo bytecode blocking；缺失 resource 字段、未知 resource id 或 hash mismatch 也会 blocking，匹配时作为公开 synthetic 中间层证据通过。`XFIR` 只接受 verified exact wrapper 中的 RIFF/RIFX payload，wrapper size 必须覆盖整个文件，并只写 decoded hash/size；opaque、压缩、尾随未验证 bytes 或不可读 Shockwave container 必须 blocking，不能退回 RIFF/RIFX 线性扫描。缺 child resource 抽取结果或 hash 冲突也必须 blocking。

补充：`tsuinosora.conversion_manifest` release gate coverage 现在要求 `resources` 至少包含一条 converted resource evidence；route 全部 covered 但 `resources` 为空会 blocking，避免用路线 coverage 冒充真实素材转换证据。每条 resource 必须带 source/native 相对路径、classification、source hash、converted hash 和正 byte size。

补充：`tsuinosora.director_lingo_map.v1` 也记录 `Lctx` entry count/table hash；`Lctx` 和 `Lnam` 都只作为脱敏结构证据，不输出 table payload。Malformed `Lctx` table 或未终止 `Lnam` table 会 blocking，避免把坏 Lingo table 当作 route coverage 前置证据。

补充：`tsuinosora.director_cast_map.v1` 现在会阻断同一 `CASt` 被多个 `CAS*` library/slot 绑定的冲突，避免 `tsuinosora.cast_source_map_report.v1` 用不唯一 member 生成素材/route evidence。

补充：`tsuinosora.cast_source_map_report.v1` 会阻断手写 cast map 或外部 director cast map sidecar 中的 payload、正文或 bytecode 字段；diagnostic 只保留字段路径，不保留字段值。

补充：TsuiNoSora package section release gate 现在会统一阻断 `tsuinosora.*` section 中的 payload-like 字段泄露；正文、脚本文本、source text、content、payload body、bytecode 和 source payload 不能进入发布包报告，`redaction.payload: omitted` 除外。

补充：`tsuinosora.asset_analysis` release gate 现在还要求至少一条 analyzed asset evidence；空 `assets` 即使 `status: pass` 也会 blocking。

补充：`tsuinosora.director_cast_map.v1` 支持显式 `tsuinosora.director_cast_member_metadata.v1` 的脱敏读取，覆盖 kind、route id、command id、anchor、bounds 和 metadata hash；这些字段会继续进入 `tsuinosora.cast_source_map_report.v1`，普通 `CASt` payload 不会被输出。

补充：`tsuinosora.reference_evidence` 对应的 `tsuinosora.visual_reference_report.v1` 现在会校验默认 `Title.png`/`Game.png` 的固定尺寸和 hash；缺文件、PNG 不可读、hash mismatch 或 dimensions mismatch 会 blocking，report 不新增商业截图输出。

补充：Director cast metadata 的 anchor/bounds 必须是可验证数值；anchor 非数值或 bounds 负尺寸会 blocking，避免布局证据缺失后继续生成 source-map evidence。

补充：Director cast metadata 中 `kind: character_atlas` 必须携带 parts；part id、pose、expression、anchor、crop、layer、mouth/eye state compatibility 和 fallback 会在 Director cast report 与 cast source-map report 中保留为脱敏证据。

补充：`tsuinosora.route_graph_report.v1` 和 `tsuinosora.script_source_map_report.v1` 现在会阻断同一 `route_id` 的 terminal/choice signature 冲突，避免不唯一 route coverage 进入 NativeVN package input。

补充：route graph 和 script source-map report 也会阻断同一 route 内的重复 choice id，避免重复选择证据写入 `.astra` option key 或 scenario `player_input choose`。

补充：Stage 3 gate 的 script source-map fallback 只在 route graph 缺失时使用；坏 route graph sidecar 的 payload、unsafe symbol、coverage 或 duplicate diagnostic 会继续阻断 conversion，不能被 fallback 覆盖。

补充：`local-gate` 当前不允许显式 routes 绕过 route evidence；routes 必须由 `tsuinosora.route_graph_report.v1` 或 `tsuinosora.script_source_map_report.v1` 派生，显式 routes 会 blocking 且不会写 NativeVN package input。

补充：`tsuinosora.script_source_map_report.v1` 对 unsupported Lingo bytecode 使用 resource 粒度覆盖；同一个 Lingo map 中只覆盖一个 `Lscr` 而遗漏其它 `requires_bytecode_reader` resource 时必须 blocking。

补充：`tsuinosora.director_resource_map.v1` 会把 `mmap` free entry 计入 `free_resource_count`，但不会把它们当作有效 resource、tag coverage 或 payload evidence；这用于避免旧 Director free-list entry 让可读资源图误阻断。
`tsuinosora.native_asset_rearrange_report.v1` 当前作为本地 gate evidence，证明 Asset analysis pass 后写入 `local_work_root/native-assets/` 的 source/native 相对路径、classification、source hash、converted hash 和 byte size；该 report blocked 时 conversion report 必须 blocked。

NativeVN package input 当前保留 route graph/source map 中的 sanitized choice id，生成同名 `.astra` option key 和 scenario `player_input choose.value`；多 choice route 按顺序生成连续 choice state，避免用 synthetic 单选覆盖真实 route evidence。

补充：NativeVN package input 还会对显式 routes 执行写入前校验；不安全 symbol、非 covered coverage、重复 choice 或冲突 route signature 会阻断，避免无效输入生成 story 或 scenario refs。

TsuiNoSora `stage3-gate` 当前会把 route-bound cast source map member 通过 source/native hash 映射到 `native-assets/`，生成 patch/windows `mount_assets` 并写入 conversion report；`local-gate` 从 report 派生 routes 时会保留 choices 和 mount evidence。

| AstraEditor | [module](../modules/editor.md) | [AI/MCP](../contracts/ai-mcp.md) | [Qt/Rust bridge](../implementation/editor-workflow.md) planned | layout preset YAML, plugin enablement state | creator workflow and plugin manager scenario | editor package gate, plugin manager gate | [creator](../manual/creator-manual.md) |
| AI/MCP | [module](../modules/ai-mcp.md) | [AI/MCP](../contracts/ai-mcp.md) | [AI/MCP API](../implementation/ai-mcp-runtime.md), [provider profiles](../implementation/ai-provider-profiles.md), [runtime memory](../implementation/runtime-ai-director-memory.md), [MCP context](../implementation/mcp-context-tooling.md) planned | audit log, encrypted debug trace, AI draft sidecar, runtime memory ledger, Context Pack, `ai.model_bundle_manifest`, `ai.generated_artifact.*` save sections | trusted session, runtime director, memory compaction, MCP context scenario, ONNX ModelBundle package/VFS lookup | provider profile, ONNX ModelBundle, runtime pack/VFS, fixed EP evidence, generated artifact save, runtime memory policy, context permission, provider-free replay | [plugin guide](../manual/plugin-developer-guide.md), [creator](../manual/creator-manual.md) |
| AstraEMU | [module](../modules/astra-emu.md), [family research](../emu/README.md), [runtime framework](../implementation/astraemu-legacy-runtime-framework.md) | [legacy runtime provider](../contracts/astraemu-ipc.md), [media](../contracts/media.md), [script](../contracts/script-vn.md) | Manager + RuntimeWorld planned, `LegacyRuntimeProvider` facade planned, auto probe planned, Trusted Luau planned, `TranslationProvider` planned, FilterGraph preset planned, Artemis family plugin planned | local case report, legacy archive formats, `LegacySnapshotEnvelope` section, text dump policy, filter preset binding | Artemis full-flow scenario, provider session replay, auto probe, trusted script, text pipeline, filter preset | legacy runtime provider gate, auto probe gate, trusted script isolation, text redaction, filter preset gate | [operator](../manual/operator-guide.md) |
| Platforms | [platforms](../platforms/README.md) | [release](../contracts/release-gate.md) | `Engine/Source/Platform/astra-platform` implemented; Windows host smoke, Web browser smoke, WMF provider and WebCodecs token provider implemented; Linux/macOS/iOS/Android moved to [Stage 6](stages/stage-6-platform-completion.md), [host APIs](../implementation/platform-host.md) | `astra.platform_capability_report.v1`, `astra.platform_eligibility.v1`, platform smoke DTO with evidence | `cargo test -p astra-platform`, `cargo test -p astra-platform-windows`, `cargo test -p astra-platform-web`, `cargo test -p astra-platform-web --target wasm32-unknown-unknown --no-run`, `wasm-pack test --headless --chrome Engine/Source/Platform/astra-platform-web`, `cargo test -p astra-cli --test target_platform` | Windows `renderer.wgpu_surface`/WMF/WASAPI/Known Folder RW smoke, Web browser media/storage/package source smoke, release profile platform-report blocking, required smoke validation, SDK status | [operator](../manual/operator-guide.md) |
