# Product Open Work

## P0

- 建立 Rust workspace、fixed stable toolchain、`tools/check_docs.py` 和基础 CI。
- 实现 `astra-core`、`astra-runtime`、`astra-test` 的最小 API。
- 定义 binary container header、section table 和 postcard payload reader/writer。
- 建立 plugin descriptor validator 和 `abi_stable` loader skeleton。

## P1

- `.astra` parser/compiler 到 CompiledStory IR。
- headless YAML scenario runner 和 release report writer。
- wgpu Renderer2D provider、headless capture provider、cosmic-text provider。
- Qt/QML Editor shell 与 PIE bridge。

## P2

- Runtime AI committed output、Trusted session audit、MCP tool descriptor。
- AstraEMU Manager IPC prototype 和 KrKr core scaffold。
- 移动/Web host module capability report。
